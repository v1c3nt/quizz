{% extends 'base.html.twig' %}

{% block title %}team {{ crew.name }}{% endblock %}

{% block body %}
<style>

</style>

<div class="bubble col-8 offset-2">

    <h3 class="m-1 border border-primary brad p-2 bubble-title"><u>les membres</u> </h3>
</div>
<main class="bubble col-8 offset-2">

    <div>
        <div class="brad m-1">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">avatar</th>
                        <th scope="col">Nom</th>
                        <th scope="col">ancienneté</th>
                        <th scope="col">role</th>

                    </tr>
                </thead>
                <tbody>
                    {% for m in crew.members %}
                    <tr>
                        <th scope="row">
                            {% if null != m.user.avatar %}
                            <img src="{{ asset('/images/vince.jpg') | imagine_filter('mini_square') }}" alt="avatar de {{ m.user.username }}"
                                class="brad">
                            {% else %}
<img src="{{ asset('/images/avatar1.png') | imagine_filter('mini_square') }}" alt="avatar de {{ m.user.username }}" class="brad">
                            {% endif %}

                        </th>
                        <td>{{ m.user.userName }}</td>
                        <td>{{ m.createdAt|date('d/m/Y') }}</td>
                        {# TODO codé en dur a modifier #}
                        <td>{{ m.roleCrew.name }}</td>

                    </tr>
                    {% endfor %}
                </tbody>
            </table>
            <span href="#" class="text-primary h3">
                <i class="fas fa-sad-tear  mr-1"></i>
            </span>
            Tu ne encore jamais créer de Quizz ?
            <p><i class="fas fa-frown"></i> c'est dommage ça prends max 10 minutes </p>

            <a href="{{ path('new_quizz') }}">Si tu veux tester !</a>

        </div>
    </div>

</main>
</div>
</div>
{% endblock %}