{{ form_start(form, {'action': path('questions_quizz', {'id': quizz.id, 'nbr': nbr } ) } ) }}
{% dump('form') %}
{{ form_widget(form) }}
<button class="btn btn-success" type="submit">
    {% dump(quizz) %}
    allé question suivante !
</button {{ form_end(form) }}